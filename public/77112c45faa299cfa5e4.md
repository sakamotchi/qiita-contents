---
title: aws cli 複数アカウント切り替え(MAC)
tags:
  - Mac
  - aws-cli
private: false
updated_at: '2020-01-16T13:27:59+09:00'
id: 77112c45faa299cfa5e4
organization_url_name: null
slide: false
ignorePublish: false
---
複数アカウントで aws cli を使うとき、--profileを指定してコマンドを実行するなどの方法がある。
direnvを使ってディレクトリ単位で付けないようにしてみた。

## 参考

https://docs.aws.amazon.com/ja_jp/cli/latest/userguide/cli-configure-profiles.html

## 手順概要

1. プロファイルを作成する。
2. direnvでAWS_PROFILE環境変数をディレクトリごとに設定する

## 手順詳細

### 1. プロファイルを作成する

personalってプロファイルを作成する想定だと aws configureで下記の入力をする。
各値は、自分のものを入力する。

```shell
~ $ aws configure --profile personal
AWS Access Key ID [None]: XXXXXXXXXXXXXXX
AWS Secret Access Key [None]: XXXXXXXXXXXXXXXXXXXX
Default region name [None]: ap-northeast-1
Default output format [None]: json
```

### 2. direnvでAWS_PROFILE環境変数をディレクトリごとに設定する

各ディレクトリに .envrc ファイルを作成して、下記の内容を記載する。

```shell
export AWS_PROFILE=personal
```
